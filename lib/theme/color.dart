import 'package:flutter/material.dart';

// Ana renkler
Color bg = const Color(0xFF0A1015); // Koyu arka plan
Color cColor = const Color(0xFF151B21); // Kart arka planı
Color navColor = const Color(0xFF0D1318); // Navigasyon rengi

// Vurgu renkleri
Color defaultColor = const Color(0xFF2EE6A8); // Ana vurgu rengi (turkuaz)
Color secondaryColor = const Color(0xFF1B8F6B); // İkincil vurgu
Color accentColor = const Color(0xFF30D5C8); // Aksan rengi

// Metin renkleri
Color textColor = Colors.white;
Color textColorSecondary = Colors.white70;
Color textColorMuted = Colors.white38;

// Gradyan renkler
List<Color> primaryGradient = [
  const Color(0xFF2EE6A8),
  const Color(0xFF30D5C8),
];

// Durum renkleri
Color successColor = const Color(0xFF2EE6A8);
Color errorColor = const Color(0xFFE65757);
Color warningColor = const Color(0xFFE6A82E);

// Gölge rengi
Color shadowColor = const Color(0xFF2EE6A8).withOpacity(0.15);

// Kart kenarlık rengi
Color borderColor = const Color(0xFF2EE6A8).withOpacity(0.1);
